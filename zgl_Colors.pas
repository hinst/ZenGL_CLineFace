unit zgl_Colors;

{$mode objfpc}{$H+}

interface

uses
  Classes, SysUtils;

const
  clBlack   = ($000000);
  clMaroon  = ($000080);
  clGreen   = ($008000);
  clOlive   = ($008080);
  clNavy    = ($800000);
  clPurple  = ($800080);
  clTeal    = ($808000);
  clGray    = ($808080);
  clSilver  = ($C0C0C0);
  clRed     = ($0000FF);
  clLime    = ($00FF00);
  clYellow  = ($00FFFF);
  clBlue    = ($FF0000);
  clFuchsia = ($FF00FF);
  clAqua    = ($FFFF00);
  clLtGray  = ($C0C0C0); // clSilver alias
  clDkGray  = ($808080); // clGray alias
  clWhite   = ($FFFFFF);
  StandardColorsCount = 16;

  // extended colors
  clMoneyGreen = ($C0DCC0);
  clSkyBlue    = ($F0CAA6);
  clCream      = ($F0FBFF);
  clMedGray    = ($A4A0A0);

implementation

end.

